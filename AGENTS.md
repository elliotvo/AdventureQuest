# AGENTS.md

## Cursor Cloud specific instructions

This is a freshly initialized repository ("AdventureQuest — My first game") with no application code, dependencies, or services yet. The repo contains only a `README.md`.

- **No build system, package manager, or framework** is configured.
- **No services** need to be started.
- **No tests or lint** tooling is present.
- **No update script dependencies** — the update script is a no-op (`true`).

When application code is added, update this section with relevant dev environment instructions (how to install deps, run, test, lint, build).
